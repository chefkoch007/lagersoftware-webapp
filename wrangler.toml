name = "lagersoftware-webapp"
compatibility_date = "2024-01-01"
pages_build_output_dir = "."

# KV binding "SCAN_KV" ist im Cloudflare Pages Dashboard konfiguriert:
# Pages Projekt → Settings → Functions → Bindings → KV namespace → SCAN_KV
